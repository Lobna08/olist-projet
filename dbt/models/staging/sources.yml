version: 2

sources:
  - name: raw
    schema: raw
    description: "CSV Olist chargés tels quels par src/ingestion/load_raw.py"
    tables:
      - name: orders
        columns:
          - name: order_id
            tests: [unique, not_null]
      - name: order_items
      - name: order_payments
      - name: order_reviews
      - name: customers
        columns:
          - name: customer_id
            tests: [unique, not_null]
      - name: products
        columns:
          - name: product_id
            tests: [unique, not_null]
      - name: sellers
        columns:
          - name: seller_id
            tests: [unique, not_null]
      - name: geolocation
